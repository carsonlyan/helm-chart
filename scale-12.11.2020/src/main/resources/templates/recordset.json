{
  "Comment": "Update CNAME record for ${clusterDomain}",
  "Changes": [
    {
      "Action": "UPSERT",
      "ResourceRecordSet": {
        "Name": "*.${clusterDomain}",
        "Type": "CNAME",
        "TTL": 300,
        "ResourceRecords": [
          {
            "Value": "${ingressHostname}"
          }
        ]
      }
    }
  ]
}